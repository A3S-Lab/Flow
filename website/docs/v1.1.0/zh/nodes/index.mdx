---
title: 节点目录
description: 查看 A3S Flow 1.1 的 18 个作者节点、两个内部容器入口，以及节点卡片和配置面板。
---

# 节点目录

A3S Flow 1.1 提供 18 个可以直接放到画布上的节点。选好节点以后，你可以在 Playground 中填写配置、连接端口并校验整张图，也可以通过 CLI 生成相同结构。每个节点的字段、默认值、端口、运行绑定和持久身份规则都来自同一份 manifest。

`iteration-start` 与 `loop-start` 是容器内部入口。它们由编辑器随容器创建，不出现在节点选择器中，也不能放到顶层画布。

## 节点工作台

下面的工作台使用 `@a3s-lab/flow-ui` 的公开组件。选择一个节点后，画布卡片和右侧配置面板会读取同一个节点对象。标题、说明和字段修改会立即反映到预览中。等待、回调、批次、子工作流和容器使用各自的专用控件。

<NodeConfigLab />

### 查看完整 manifest 契约

选中任务节点后，打开面板底部的 Manifest 契约，就能查看驱动编辑器的同一份
元数据。这里会列出 manifest 版本、所有者、角色、运行绑定、稳定 ID 绑定和标记，
还会按声明顺序列出全部输入端口、输出端口和字段。每个字段都会显示 A3S UI 控件、
语义分组、必填状态、高级状态、只读状态、条件显示、当前可见性、当前值状态，以及
manifest 属性的名称和值。敏感字段的值会被遮蔽。

这个契约视图直接投影当前节点，没有另起一套模式。表单、校验和契约视图共用同一个
字段显示条件。文档操作会导出节点配置，当前因条件隐藏的值也会保留，保存后的工作流
仍然可以无损恢复。

## 六个分组

| 分组       | 节点                                                                                                                              | 负责的事情                             |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 流程入口   | [开始](./start)、[条件分支](./condition)                                                                                          | 接收输入并选择确定性的控制分支         |
| 任务与工具 | [执行任务](./step)、[批量任务](./batch)                                                                                           | 调用宿主任务并保存结果、错误和重试状态 |
| 等待与审批 | [定时等待](./wait)、[回调与审批](./hook)、[信号等待](./signal)                                                                    | 释放 worker，等待时间或外部输入        |
| 子流程     | [外部任务引用](./child-operation)、[子工作流](./child-workflow)、[子工作流批次](./child-workflows)、[后续运行](./continue-as-new) | 管理长任务引用、第一类子运行和历史续段 |
| 运行状态   | [运行进度](./progress)、[成功结束](./complete)、[失败结束](./fail)、[取消运行](./cancel)、[超时结束](./timeout)                   | 写入可检查状态并关闭运行段             |
| 子画布容器 | [遍历集合](./iteration)、[条件循环](./loop)                                                                                       | 建立独立图作用域，约束父子关系和连线   |

## 按眼前任务选择节点

收到输入以后要执行一次宿主任务，使用[执行任务](./step)。同一批任务需要按成员保存结果和重试状态，使用[批量任务](./batch)。两者都要求宿主注册真实任务处理器。

流程需要停下来等某个时间，使用[定时等待](./wait)。等待审批或一次回调，使用[回调与审批](./hook)。已经有业务消息通道，只需要按名称恢复运行，使用[信号等待](./signal)。这三个节点都会释放 Worker。

一项工作需要自己的历史、状态和取消策略，使用[子工作流](./child-workflow)。需要对一组输入重复执行相同子图，使用[遍历集合](./iteration)。重复次数由条件决定，使用[条件循环](./loop)。普通连线仍然保持无环。

## 运行绑定

有 `runtimeBinding` 的节点会编译成 A3S Flow 的持久运行命令。`flow.start` 和 `flow.condition` 在图执行层处理。`iteration` 与 `loop` 负责子画布结构，宿主决定如何把每轮工作编译成步骤或子工作流。

任务和等待类节点通常使用图节点 ID 作为持久身份。批量节点还会把成员 key 加入身份。已有运行开始后，修改这些 ID 会让重放无法匹配原历史。

每个节点的独立页面都列出以下内容。

- 适用场景和运行方式
- 全部字段、默认值、控件、范围、可选值和显示条件
- 控制端口与数据端口
- 运行绑定和持久身份规则
- 完整节点 JSON、CLI 命令和 Skill 提示
- 与恢复、重试、取消和容器作用域有关的注意事项

## 配置没有通过校验时

先按错误路径找到节点和字段。字段错误通常来自必填值为空、数字超出范围或条件字段没有同步更新。连线错误通常来自端口类型不匹配、输入端口已经占用、跨越容器作用域或形成环路。

不要通过删除未知字段来压下错误。先在节点独立页面核对字段和端口，再回到 Playground 或 CLI 重新校验。已有运行开始以后，还要保留节点 ID。步骤、Hook、进度和子运行会用它匹配已经提交的历史。

## 从 CLI 核对清单

安装包中的清单与前端组件一起发布。先查看当前版本，再创建节点。

```bash
a3s-flow nodes --pretty
a3s-flow node flow.step --pretty
a3s-flow new flow.step --id charge-order --pretty
```

把节点加入图后，运行 `validate`、`compile` 和 `digest`。CLI 会检查字段、端口、作用域、环路和确定性执行顺序。
